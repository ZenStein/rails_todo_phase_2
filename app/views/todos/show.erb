<h1>Lists:</h1><br>

<div ng-controller="todosController">

<input type="text" ng-model="newTask.name"><button ng-click="add()">add task</button><br>
  <div ng-repeat="todo in todos track by $index">
    <button id="button{{todo.id}}">delete</button> {{$index}}. <a href="#/{{todo.id}}/todos"> {{ list.name }} </a><label> {{list.todos_count}} </label>
  </div>

</div>

<a href="#/lists">Back to Lists</a>


